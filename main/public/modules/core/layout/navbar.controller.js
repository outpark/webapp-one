(function() {
    'use strict';
    var module = angular.module('core');

    module.controller('NavbarController', function($scope, $document, $state, $rootScope, $log) {
        $log.debug("Navbar controller active");
        return;
    });
}());